# Core web framework
fastapi==0.141.1
uvicorn[standard]==0.53.0

# LangGraph + LLM providers (provider-agnostic via app/llm.py)
langgraph==1.2.11
langgraph-checkpoint==4.2.0
langgraph-checkpoint-postgres==3.1.2
langchain-core==1.6.3
langchain-openai==1.6.2
langchain-anthropic==1.7.2

# Vector store (embedded, no server/docker) for the knowledge-base tool.
# NOTE: milvus_lite ships Linux/macOS binaries only -- this installs and
# runs fine in the backend Docker image / CI, but will fail to import the
# milvus_lite extra on native Windows Python. Run via Docker or WSL on
# Windows. See README "Docker / infra" section.
pymilvus[milvus_lite]==3.0.1

# Free web search, no API key
ddgs==9.16.0

# Database
SQLAlchemy==2.0.54
alembic==1.20.0
psycopg[binary]==3.3.5

# Settings / config
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3

# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
